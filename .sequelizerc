const path = require('path');

module.exports = {
  'config': path.resolve('db.js'), // caminho para o arquivo db.js na raiz
  'models-path': path.resolve('models', 'model.js') // caminho para o arquivo de modelos
};
